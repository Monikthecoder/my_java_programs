#                                       ### ***    GREEDY ALGORITHMS   *** ###
### purpose :- introduction to the greedy algorithm

### intution :- if you already solve a problem then do not visit or calculate the answer and directly choose a greedy way to make solution of the program by rudicing the extra caclulations and that is a way to make the program more efficient in two words "choose best choice (optimal solution)


*** 1. optimization :- mostly used when we need to find a max or min ***
*** 2. Sorting :- we use sorting in most of the greedy algorithms not every *** 
*** 3. FixedRule :- they have no fixed rule to apply as example think of a binary search it needs to follow int mid = low + (low - right) / 2  whereas greedy algorithms has no fixed rules to maintain a fixed type of things ***

*** 4. No realization :- there are possibility that we have used this approach and we never realized not only in this chapter but in the other problems might we need to apply the greedy approachs so you need to make sure that you practice more question rather than copy them without any intuation ***

### IT IS EASY TO CODE BUT HARD TO APPLY THE LOGIC AND UNDERSTANDING OF CERTAIN TOPICS WITHOUT ANY GUIDANCE ###

## Definition

*** Greedy algorithms is the problem solving technique where we make the
locally optimum choice at each stage & hope to achieve a global optimum. ***

***
*** Pros ***
'''
Simple & Easy

Good enough TC
'''
*** Cons ***
'''
A lot of time, global
optimum is not achieved
'''
***